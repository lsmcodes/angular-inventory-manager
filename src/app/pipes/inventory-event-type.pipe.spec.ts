import { InventoryEventTypePipe } from './inventory-event-type.pipe';

describe('InventoryEventTypePipe', () => {
  it('create an instance', () => {
    const pipe = new InventoryEventTypePipe();
    expect(pipe).toBeTruthy();
  });
});
